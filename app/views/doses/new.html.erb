<div class="wrapper">
  <div class="container">
    <h1> Detail the recipe of <%= @cocktail.name %></h1>
    <%= simple_form_for [@cocktail, @dose] do |f| %>
      <%= f.input :description %>
      <%= f.association :ingredient %>
      <%= f.submit "Add a dose" %>
    <% end %>
  </div>
</div>
